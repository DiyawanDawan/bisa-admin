import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Notifikasi | BISA Admin",
};

export default function NotificationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
